# 财政沙盘 · 开发大纲

目标：把四张图（①公共债务构成与规模 ②2026 年一般公共预算安排 ③2025 年一般公共预算执行 ④财政"四本账"）做成一个联动模拟器。
任何一个数字都能点开看到「公式 / 读法 / 代入」，调任何参数都能看到沿依赖图传导的全部变化。

## 设计原则

1. **公式即计算**：每个派生量用一条表达式字符串定义（如 `rc25 * (1 + g_rc)`），同一条表达式既用于求值，也用于渲染成公式卡片。显示的公式不可能与实际算法不一致。
2. **基线 = 原图**：所有输入参数的默认值取自四张图；单元测试逐项核对原图数字（见 `tests/reconcile.test.mjs`）。
3. **公式分级**：每条公式标注性质——`会计恒等式`（图中直接成立的加总关系）、`校准关系`（参数由图中数据反推）、`假设`（图中没有、需要外部假设的参数，均可调）。
4. **平衡规则可切换**：预算恒等式里必须有一项是"余项"。模拟器让用户选择由谁兜底（赤字、支出、调入资金……），切换规则不改变当前数值，只改变之后谁来吸收冲击。

## 模块

| 模块 | 对应图 | 年份 | 内容 |
| --- | --- | --- | --- |
| 2025 执行 | 图③ | 2025 | 16 个税种 → 税收 → 扣出口退税 → +非税 → 收入；13 类支出；差额 = 赤字 + 调入；分税制拆出中央/地方收入 |
| 2026 预算 | 图② | 2026 | 中央收入 + 中央赤字 + 调入 = 中央支出 = 本级 + 转移支付 + 预备费；地方收入 + 转移支付 + 地方赤字 + 调入 = 地方支出；赤字率 |
| 债务 | 图① | 2025 末 → 2026 末 | 国债、地方一般债/专项债、城投隐性债务三种口径；新增债务、置换化债、付息 |
| 四本账 | 图④ | 2021 | 一般公共预算、政府性基金、国有资本经营、社保基金之间的调入调出、补贴、稳定调节基金；合并口径广义赤字 |
| 十年推演 | ②+① | 2026–2035 | 债务动态 Δd = d·(r−g)/(1+g) + 基本赤字率 + 其他债务融资 |

跨图连接（全部是公式）：

- ③→②：2026 预算数 = 2025 执行数 × (1 + 增速)；国防支出直接取图③的 2025 值。
- ③→①：2025 年赤字偏离实际值时，2025 年末债务余额同步偏离（反事实调整）。
- ②→①：2026 年末余额 = 期初 + 中央/地方赤字 + 专项债 + 特别国债 + 置换。
- ①→②：中央债务付息 = 国债平均付息率 × 国债余额。
- ④↔②③：四本账是 2021 年数据，与 2025/2026 年模块不共享数值，只共享同一套恒等式（"四本账"页的三年对照表、"公式手册 → 跨图连接"矩阵都如实标明了这一点）。

## 阶段

- [x] P0 数据核对：四张图的全部数字及加总关系
- [x] P1 公式引擎（解析器、依赖图、拓扑求值、公式渲染）+ 单元测试
- [x] P2 模型规格（五个模块、平衡规则）+ 对账测试 + 恒等式随机测试
- [x] P3 界面骨架：顶栏指标、标签页、参数面板、公式卡片、传导链列表
- [x] P4 图表：2025/2026 桑基图、债务分层图、四本账结构图、推演折线/分解图
- [x] P5 系统总览图（四张图如何相连）+ 传导路径图（DAG）
- [x] P6 好玩的部分：情景预设、挑战关卡、先猜后算小测验
- [x] P7 公式手册 + 原图对账表 + README
- [x] P8 打包单文件、Playwright 端到端测试、截图检查、手机布局、深色模式
- [x] P9 发布为 Artifact

## 迭代记录（P10 起）

- [x] 2025 桑基图排版；四本账箭头改走面板间空隙；深色模式对比度
- [x] 公式卡片"为什么变了"：逐项贡献（加减式精确，乘除式列出交互项）+ 主要传导路径
- [x] 敏感度龙卷风图；反向求解（二分法，只在参数允许区间内）
- [x] 撤销/重做；情景 A/B 对比；情景代码复制与载入
- [x] 极端值加固：全部滑杆取两端、随机组合都不产生 NaN（修复推演中"地方赤字份额"除以零）
- [x] 联动高亮；桑基图悬停聚焦；键盘可达
- [x] 讲解模式（6 条逐步讲解，文字里的数字全部现算；声称"不变"的步骤有断言）
- [x] 自动生成 docs/MODEL.md 并用测试保持同步；跨图连接矩阵；三年恒等式对照
- [x] 性能：去掉每帧强制重排、次要面板节流、公式手册原地刷新（每次拖动约一帧）
- [x] 推演：存量利率部分重定价；不确定性扇形图（300 次抽样）
- [x] 名词解释、公式实验室、参数搜索、复制公式
- [x] 中央吸收项新增"本级各项等比例"；规则对比表（六种规则并排）
- [x] 挑战 8 关（星级按动用旋钮数）、测验 13 题、事件卡 12 张（对冲方案由反向求解给出）
- [x] 复制本次分析（纯文本摘要 + 情景代码）；快捷键帮助
- [x] 总览传导回放（按拓扑序逐站点亮，箭头先于它进入的方框）
- [x] 公式卡片按"你改的参数"做 Shapley 归因（≤ 10 个参数精确计算）
- [x] 按测试报告修复：字体不阻塞首屏、收入为 0 时的 Infinity、相图/扇形图提速 4 倍、查找同义词与加权、复位按钮 28px、相图红蓝配色、旧浏览器兼容
- [x] 好玩：随机任务（无尽模式、今日任务、连胜、计时、晒战绩）、24 枚成就徽章、彩纸庆祝
- [x] 复测后修复：旧浏览器的 color-mix 降级统一放进 @supports 块（含 var() 的声明不能靠"先写纯色"兜底），影响矩阵改画数据条；320 px 下名词页溢出；任务卡"达成"章不再盖住按钮
- [x] 全局查找（/ 或 Ctrl+K）、历史状态列表、CSV 导出、"我想知道"问题导航、第 7 条讲解与第 8 个关卡
- [x] 细节：代入式有效数字、切换规则的浮点残差、对比度达 WCAG AA、手机布局与减号统一
- [x] 影响矩阵路径分解（链式法则，逐条路径贡献 + 与直接重算对照）
- [x] 双目标求解（牛顿法，奇异时说明原因）；随机操作测试（fuzz）
- [x] 双参数相图：8 张预设（含加法对照组）、等值线、取舍比率与交互项的代入式、点格子应用、键盘可达

## 测试

- 单元测试（`npm test`）：引擎、原图 107 项对账、恒等式随机测试（300 组）、规则切换不改变数值、极端值、归因、敏感度、反向求解、扇形图、情景代码、关卡可解、测验答案、讲解步骤、事件卡、模型文档同步。
- 端到端（`npm run e2e`）：真实浏览器中逐页操作，含手机宽度与深色模式（39 项）。
- 随机操作（`node tests/e2e/fuzz.mjs 400 7`）：随机切页、改参数、换规则、点按钮，检查脚本错误和 NaN/undefined 文本。
